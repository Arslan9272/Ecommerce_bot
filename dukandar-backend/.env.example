# Postgres in production:
DATABASE_URL=postgresql+asyncpg://dukandar:dukandar@localhost:5432/dukandar
# For local dev without Postgres you can use:
# DATABASE_URL=sqlite+aiosqlite:///./dukandar.db

CORS_ORIGINS=http://localhost:5173

# Cloud TTS (server-side only — never exposed to the browser)
ELEVEN_API_KEY=
ELEVEN_VOICE_ID=
TTS_LANG=ur-PK
